# 测试报告

## 云南自然灾害应急协同决策平台

| 版本 | 日期 | 状态 |
|------|------|------|
| V1.0 | 2024-07-24 | 初稿 |

---

## 一、测试概述

### 1.1 测试目标

验证平台各模块功能的正确性、稳定性和完整性，覆盖后端业务逻辑、API端点、前端页面渲染和组件交互。

### 1.2 测试环境

| 项目 | 环境 |
|------|------|
| 操作系统 | Windows 11 / Ubuntu (GitHub Actions CI) |
| Python | 3.12 |
| Node.js | 20 |
| 数据库 | PostgreSQL 15 + PostGIS (集成测试) / SQLite 内存 (单元测试) |
| 测试框架(后端) | pytest 9.1 + pytest-asyncio |
| 测试框架(前端) | vitest 4.1 + @testing-library/react |
| API测试客户端 | httpx (ASGI transport) |
| CI/CD | GitHub Actions |

### 1.3 测试分层策略

```
┌─────────────────────────────────────┐
│         前端组件测试 (vitest)         │  ← 10个页面 + 组件渲染验证
├─────────────────────────────────────┤
│         API 端点集成测试 (pytest)      │  ← 鉴权、CRUD、状态流转
├─────────────────────────────────────┤
│        服务层单元测试 (pytest)         │  ← Agent/AI/采集/调度/审计
├─────────────────────────────────────┤
│      核心层单元测试 (pytest)           │  ← 安全/FSM/配置/Haversine
└─────────────────────────────────────┘
```

---

## 二、测试执行结果

### 2.1 后端测试 — pytest

**运行命令：**
```bash
cd backend
pytest tests/ -v                   # 单元测试 (无需数据库)
pytest tests/ -v -m integration    # 集成测试 (需要PostgreSQL)
```

#### 单元测试结果（74 个）

```
tests/core/test_security.py ............ 12 passed
tests/services/test_services.py ........ 14 passed
tests/services/test_services_extended.py .................................................. 48 passed
```

| 测试类 | 测试数 | 覆盖模块 |
|------|:---:|------|
| TestSecurity | 3 | bcrypt密码哈希、JWT创建/解码 |
| TestIncidentFSM | 6 | 灾情状态机4状态转移、合法/非法路径 |
| TestHaversine | 3 | 地球表面距离计算（零距离、已知距离、引擎版） |
| TestDeepSeek / TestDeepSeekClient | 5 | SYSTEM_PROMPTS完整性、单例模式、客户端初始化 |
| TestCollectors / TestCollectorPlugins / TestGdacsCollector | 15 | USGS/天气/预警/GDACS采集器初始化、城市列表、基类抽象、区域过滤、等级映射 |
| TestResourceTemplates / TestResourceAutoDispatcher | 6 | 5类灾种模板覆盖率、空资源/有资源自动匹配 |
| TestAudit / TestAuditExtended | 4 | log_action、get_audit_logs带/不带筛选 |
| TestResourceScheduler / TestResourceSchedulerExtended | 6 | 锁定/释放/冲突检测/批量释放 |
| TestRAG / TestRAGExtended | 4 | ILIKE关键词匹配、match_plans |
| TestConfig / TestConfigExtended | 4 | Settings默认值、CORS解析、JWT参数 |
| TestIngestionPipeline | 6 | run_ingestion双→三管线(含GDACS)、异常处理、事件查询、GDACS入库 |
| TestCollectorOrchestration | 8 | 缓存数据获取、collect_all四采集器并行/异常降级、GDACS实例 |
| TestAgentRunner | 4 | create_agent_run、finish_agent_run成功/失败 |
| TestAITemplateEngine | 2 | 模板生成结构完整性、预案参考注入 |

#### 集成测试结果（10 个，需 PostgreSQL）

```
tests/api/test_api.py ............... 10 passed
```

| 测试类 | 测试数 | 覆盖端点 |
|------|:---:|------|
| TestHealth | 1 | GET /api/health |
| TestAuth | 4 | POST /api/auth/login (成功/错误密码/不存在/未登录) |
| TestIncidentsAPI | 3 | GET/POST /api/incidents (空列表/鉴权拦截/不存在) |
| TestResourcesAPI | 1 | GET /api/resources (空列表) |
| TestStatisticsAPI | 1 | GET /api/statistics (结构验证) |

### 2.2 前端测试 — vitest

**运行命令：**
```bash
cd frontend
npx vitest run
```

#### 测试结果（12 文件，42 测试）

| 测试文件 | 测试数 | 验证内容 |
|------|:---:|------|
| `Login.test.tsx` | 5 | 表单标题、用户名/密码输入框、登录按钮、默认账号提示 |
| `Dashboard.test.tsx` | 1 | 页面渲染 + API mock |
| `IncidentReport.test.tsx` | 4 | 灾情上报标题、提交按钮、上报记录卡片、地图渲染 |
| `IncidentReview.test.tsx` | 3 | 审核页标题、4个Tab标签(待核验/已确认/处置中/已归档)、表格 |
| `PlanWorkbench.test.tsx` | 4 | 方案生成标题、AI生成按钮、历史方案区、空占位提示 |
| `ResourceBoard.test.tsx` | 5 | 5步指引、资源看板、调度管理、添加资源/新建调度按钮 |
| `KnowledgeBase.test.tsx` | 4 | 知识库标题、添加预案按钮、搜索框、antd表格渲染 |
| `DataSources.test.tsx` | 3 | 数据源标题、添加按钮、表格渲染 |
| `AgentAudit.test.tsx` | 4 | 审计日志标题、记录表格、失败重试按钮、详情按钮 |
| `UserManage.test.tsx` | 5 | 用户管理标题、添加按钮、表格、管理员/信息员显示 |
| `MapView.test.tsx` | 2 | 加载态、带空数据渲染 |
| `api.test.ts` | 2 | API对象方法完整性、login函数类型 |

### 2.3 测试覆盖率

| 维度 | 覆盖率 |
|------|:--:|
| 后端 API 路由 | 5/12 模块 (认证/灾情/资源/统计/健康) |
| 后端服务层 | 12/13 模块 (除 seed_data 和 historical_seed 需真实DB) |
| 后端核心层 | 2/2 模块 (security + database) |
| 前端页面 | 10/10 页面 (100%) |
| 前端组件 | 2/2 通用组件 (Layout + MapView) |
| CI自动化 | ✓ GitHub Actions 每次push自动运行 |

---

## 三、AI效果测试（说明性）

### 3.1 DeepSeek方案生成

| 测试维度 | 预期结果 | 实际表现 |
|------|------|------|
| 方案结构 | 含灾情概述、响应等级、组织机构、处置措施、资源调配、注意事项 | 模板引擎始终生成6段完整结构 |
| 预案引用 | 从13份云南预案库中匹配2-3份相关预案 | ILIKE关键词匹配成功率高 |
| 历史事件注入 | 从28条历史采集事件中注入最近5条 | Haversine距离排序有效 |
| 降级机制 | DeepSeek不可用时自动切换本地模板引擎 | 降级无感，方案生成不中断 |
| SSE进度推送 | 分4阶段：分析→检索→生成→完成 | 正常推送4个阶段状态 |
| 自动资源匹配 | 按灾种自动匹配最近资源并创建调度建议 | 匹配清单包含人员/车辆/物资/避难4类 |

### 3.2 降级容错测试

| 场景 | 处理方式 |
|------|------|
| DeepSeek API超时 | 捕获异常，自动切换模板引擎 |
| 和风天气API未配置Key | 返回错误信息，不阻塞采集流程 |
| USGS地震API网络错误 | 缓存上次成功数据，下轮重试 |
| Milvus向量数据库不可用 | 自动回退到PostgreSQL ILIKE检索 |
| WebSocket断开 | 客户端指数退避重连(1s→2s→4s→...→30s) |

---

## 四、CI/CD自动化测试

### 4.1 GitHub Actions 配置

文件：`.github/workflows/test.yml`

```yaml
触发条件: push -> main 分支 / pull_request -> main 分支

Job 1: backend-test
  - ubuntu-latest + PostgreSQL 15 服务容器
  - pip install 依赖
  - pytest tests/ -v --junitxml=reports/pytest.xml
  - 生成 XML 测试报告上传为 artifact

Job 2: frontend-test
  - ubuntu-latest + Node.js 20
  - npm install
  - npx vitest run
```

### 4.2 CI 状态徽章

[![Test & Report](https://github.com/EclairFontaine/disaster-emergency-platform/actions/workflows/test.yml/badge.svg)](https://github.com/EclairFontaine/disaster-emergency-platform/actions/workflows/test.yml)

访问 https://github.com/EclairFontaine/disaster-emergency-platform/actions 查看最新运行状态。

### 4.3 执行截图

（待CI运行稳定后补充截图）

---

## 五、测试结论

### 5.1 通过标准

- 单元测试：74 个全通过 ✓
- 集成测试：10 个全通过（需PostgreSQL环境）✓
- 前端组件测试：42 个全通过 ✓
- CI自动化：配置完成，push自动触发 ✓

### 5.2 待改进项

| 项目 | 说明 |
|------|------|
| 数据库集成测试 | 当前依赖PostgreSQL服务，可增加SQLite fallback |
| WebSocket测试 | 需增加ws连接握手和事件广播的集成测试 |
| 性能测试 | 需增加locust/k6压测脚本 |
| E2E测试 | 可引入Playwright进行端到端测试 |
| AI效果量化 | 可引入方案质量评分机制 |

---

*本报告由自动化测试框架生成统计数据，2024年7月24日。*
